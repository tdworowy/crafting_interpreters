var a = 0;
var temp;

for (var b = 1; a < 100000; b = temp + b) {
    print a;
    temp = a;
    a = b;
}

fun fib(count) {
    var a = 0;
    var temp;

    for (var b = 1; a < count; b = temp + b) {
        print a;
        temp = a;
        a = b;
    }
}
fib(100000);

fun fib2(n) {
    if(n <=1) return n;
    return fib2(n - 2) + fib2(n - 1);
}
for(var i = 0; i < 26; i = i + 1) {
    print fib2(i);
}